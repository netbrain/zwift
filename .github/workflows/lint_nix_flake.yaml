name: Validate nix flake
on:
  pull_request:
    paths:
      - '**.nix'
  push:
    paths:
      - '**.nix'
jobs:
  validate-nix-flake:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v5
      - name: Install nix
        uses: cachix/install-nix-action@v31
      - name: Check nix flake for errors
        run: nix run github:oxalica/nil -- diagnostics *.nix
#     - name: Run nix linter
#       run: nix run github:nix-community/nixpkgs-lint -- *.nix
